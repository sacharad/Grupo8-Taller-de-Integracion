<%= form_for(@orders_sftp) do |f| %>
  <% if @orders_sftp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orders_sftp.errors.count, "error") %> prohibited this orders_sftp from being saved:</h2>

      <ul>
      <% @orders_sftp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
